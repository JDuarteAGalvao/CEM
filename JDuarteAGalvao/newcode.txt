skip 8 lines
line 9 begins with number of lines the ray has
points for a ray begin
and repeats

currentline = 10
nextline = 


filename = 'projectR.ray';
lineNumber = 9;
fid = fopen(filename, 'r');
currentLine = '';
try
    for k = 1:lineNumber
        currentLine = fgetl(fid);
    end
catch ME
    fclose(fid);
    rethrow(ME);
end
fclose(fid);
A= str2double(split(currentLine));
A=A(~isnan(A));
dataRange = [lineNumber, 1, A(1)+lineNumber, 3]; 
dataMatrix = readmatrix(filename, 'Range', dataRange, 'FileType', 'text');
disp(['Matrix size: ', num2str(size(dataMatrix))]);